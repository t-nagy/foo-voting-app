<Page x:Class="WPFUI.View.RegistrationCompletePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPFUI.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RegistrationCompletePage" Style="{StaticResource DefaultPage}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Row="1" Grid.Column="1" Content="Thank you for registering!" Style="{StaticResource TitleLabel}"></Label>
        <TextBlock Grid.Row="2" Grid.Column="1" Margin="0 20 0 10" Text="Please confirm your email address via the link sent to you and sign in to use the application." FontSize="{DynamicResource FontSize-Large}" TextWrapping="Wrap" TextAlignment="Center"/>
        <Button Grid.Row="3" Grid.Column="1" Command="{Binding ShowLoginCommand}" FontSize="{DynamicResource FontSize-Large}" Width="250" >Continue To Login</Button>
    </Grid>
</Page>
